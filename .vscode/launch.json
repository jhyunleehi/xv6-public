{

    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to gdbserver",
            "executable": "./kernel",
            "target": ":26000",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"

        }
    ]
}